:root { font-family: "SF Pro Display", "Segoe UI", sans-serif; color: #111827;
background: radial-gradient(circle at top right, #dbeafe, #f8fafc 55%); } * {
box-sizing: border-box; } body { margin: 0; min-height: 100vh; } .shell {
max-width: 960px; margin: 0 auto; padding: 2rem 1rem; } .shell-header { display:
flex; justify-content: space-between; align-items: center; gap: 1rem;
margin-bottom: 1.25rem; } .shell-header nav { display: flex; gap: 0.75rem; }
.shell-header a { text-decoration: none; color: #1d4ed8; font-weight: 600; }
.shell-main { background: white; border-radius: 0.9rem; box-shadow: 0 10px 40px
rgba(15, 23, 42, 0.1); padding: 1.25rem; }